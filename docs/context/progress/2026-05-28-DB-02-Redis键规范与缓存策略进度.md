# 2026-05-28 DB-02 Redis 键规范与缓存策略进度

## 1. 本次目标

在不进入服务实现代码的前提下，完成一期 MVP 阶段 Redis 缓存基线固化，明确键命名、TTL 分层、值结构建议和失效规则，为后续 `GW-01`、`PS-01`、`CS-*` 与索引类服务接入提供统一约束。

## 2. 已完成内容

- 新增 `DB-02` 正式规范文档：
  - `docs/specs/architecture/06-Redis键规范与缓存策略.md`
- 新增 `DB-02` 执行计划：
  - `docs/plans/execution/14-DB-02-Redis键规范与缓存策略计划.md`
- 已将设计基线收口到正式规范与执行计划
- 统一固化四类缓存规则：
  - 项目元数据缓存
  - 热点查询结果缓存
  - 任务状态缓存
  - 会话上下文缓存
- 明确统一键格式：
  - `ac:v1:{domain}:p:{projectId}:{entity}:{suffix}`
- 明确 TTL、保守清理和风险约束：
  - 项目元数据 15 到 30 分钟
  - 查询缓存 5 到 15 分钟
  - 任务状态 6 到 24 小时
  - 会话上下文 15 到 30 分钟

## 3. 验证结果

本轮进行了文档级一致性验证，确认：

- `DB-02` 正式规范与执行计划已落地
- 当前阶段、当前任务和仓库说明已标记 `DB-02` 完成
- 下一顺序任务已切换到 `GW-01`
- 正式规范文档已覆盖四类缓存与 TTL / 失效规则

## 4. 归档说明

`DB-02` 已完成，本文档仅保留 Redis 缓存基线规范固化事实，后续任务顺位以 `docs/context/current/` 为准。
