# 2026-05-28 文档治理与 Agent 约束改造进度

## 背景

为了支持强约束的 Agent 协作、按需加载控 token、新会话快速接续，以及后续测试、质量和审计闭环，对仓库文档结构进行了治理化重构。

## 已完成事项

- 将原 `docs/` 平铺文档迁移到分层目录：
  - `specs/product/`
  - `specs/architecture/`
  - `specs/analysis/`
  - `plans/execution/`
- 新建 `indexes/` 作为轻量入口层
- 新建 `governance/` 作为开发治理和门禁层
- 新建 `quality/` 作为测试、评审、审计层
- 新建 `context/` 作为当前状态、进度和决策层
- 创建根级 `AGENT.md` 和 `CLAUDE.md`
- 创建 3 个可复用的 workspace skills 骨架
- 将 `AGENT.md`、`CLAUDE.md` 改为中文
- 将“后续文档默认使用中文”写入入口约束与治理规则
- 补充任务边界检查、进入门禁检查、阶段自测边界检查与文档更新边界约束
- 补充治理基线阶段验收清单
- 补充模块级任务入口文件，降低新会话定位成本

## 当前收益

- 新会话不需要全量读取全部文档
- Agent 有清晰的先计划后开发约束
- 文档、计划、质量、审计形成基本闭环

## 后续建议

- 审核治理文档措辞与团队习惯是否一致
- 完成治理基线阶段验收后，将当前任务切换为具体模块任务
