# 2026-05-29 一期联调与权限闭环推进进度

## 本次目标
- 在不暂停主线推进的前提下，完成 `CS-05` 到 `CS-07`、`PS-03`、`GW-03` 的代码落地与自动化验证
- 同步建设 `IT-05`、`IT-06`、`IT-07` 的可执行联调基线
- 补齐文档与联调入口，确保“代码完成=文档完成=验证完成”

## 已完成内容
- `context-service`
- 完成上下文聚合模型 `StructuredContextBundle` 与聚合服务 `ContextAggregationService`
- 完成查询缓存服务 `QueryCacheService`，接入查询编排层
- 扩展查询接口 `POST /api/query/impact`、`POST /api/query/traceability`
- 完成查询契约与场景测试：
  - `QueryIntegrationContractTest`（`IT-05`）
  - `ContextAggregationServiceTest`（`IT-06`、`IT-07`）
- `project-service`
- 完成项目成员权限映射最小实现：
  - `POST /internal/projects/{projectId}/members/{userId}?role=...`
  - `GET /internal/projects/{projectId}/members/{userId}/access`
- `api-gateway`
- 完成项目级查询授权拦截（`GW-03`）：
  - 对 `/api/query/**` 强制校验 `X-Project-Id`
  - 无项目头返回 `400`
  - 未授权返回 `403`
  - 授权通过后放行
- `integration-tests`
- 新增 `integration-tests/run-mvp-smoke.ps1`，统一串行执行：
  - `project-service` 测试
  - `api-gateway` 测试
  - `context-service` 测试

## 验证结果
- `mvn -pl context-service -am test`：通过（30 tests）
- `mvn -pl project-service -am test`：通过（12 tests）
- `mvn -pl api-gateway -am test`：通过（6 tests）
- `powershell -ExecutionPolicy Bypass -File integration-tests/run-mvp-smoke.ps1`：通过

## 结论
- 当前代码、测试、文档已经同步到同一阶段：`CS-07 + PS-03 + GW-03 + IT-05/06/07`
- 下一步主线应进入 `IT-08`、`IT-09` 与 `IT-10` 的前后端与整链联调
