# 2026-05-29 索引管线补齐与状态机推进进度

## 本次目标
完成一期 MVP 遗留的 5 项任务：PS-04 状态机、CG-02~04 CodeGraph Runner、HI-02~04 History Indexer、KI-02~04 Knowledge Indexer、IT-01~04 联调测试。

## 已完成内容

### PS-04 异步索引触发与项目状态机
- 添加 `index_error` 列到 `app.projects` 表
- `ProjectRecord` 新增 `indexError` 字段
- `ProjectRepository` 新增 `updateStatusWithError()` 方法
- 创建 `IndexOrchestratorService`（@Async 异步执行）
- `ProjectServiceApplication` 添加 `@EnableAsync`
- 项目状态流转：CREATED → INDEXING → READY/FAILED
- `POST /api/projects/{id}/sync-indexes` 改为异步（立即返回 INDEXING 状态）

### CG-02~04 CodeGraph Runner 完整管线
- 添加 `spring-boot-starter-web` + `spring-boot-starter-jdbc` + `postgresql` 依赖
- 创建 `CodegraphIndexRepository`：symbols 和 symbol_edges 表写入
- 创建 `CodegraphIndexService`：符号提取（Java/TS/Python）、边构建（imports/calls）
- 创建 `CodegraphIndexController`：`POST /api/runner/codegraph/index` REST API
- 3 个测试通过

### HI-02~04 History Indexer 完整管线
- 添加 web + jdbc + postgresql 依赖
- 创建 `HistoryIndexRepository`：commits 和 commit_symbols 表写入（含 upsert）
- 创建 `HistoryIndexService`：Git log 解析、需求码提取
- 创建 `HistoryIndexController`：`POST /api/runner/history/index` REST API
- 6 个测试通过

### KI-02~04 Knowledge Indexer 完整管线
- 添加 web + jdbc + postgresql + validation 依赖
- 创建 `KnowledgeIndexRepository`：documents/requirements/document_links 表写入
- 创建 `KnowledgeIndexService`：Markdown 文档扫描、frontmatter 解析、需求码提取
- 创建 `KnowledgeIndexController`：`POST /api/runner/knowledge/index` REST API
- 8 个测试通过

### 文档同步
- `01-当前阶段与目标.md`：阶段退出条件更新，标记全部任务完成
- `02-当前任务状态.md`：状态更新为"一期MVP开发完成"，列出全部已完成项

## 验证结论
- 全部 7 个后端服务 69 个测试 ✅
- 前端 `pnpm build` ✅
- `docker compose config` ✅
- `run-mvp-smoke.ps1` ✅
- 一期 MVP 全部必做模块已就位

## 归档说明
本次推进完成了一期 MVP 全部遗留任务。索引管线通过 REST API 暴露，project-service 的 `ProjectIndexSyncService` 保留作为本地回退实现。