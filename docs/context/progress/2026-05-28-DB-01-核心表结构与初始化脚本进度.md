# 2026-05-28 DB-01 核心表结构与初始化脚本进度

## 本次完成

- 新增 `deploy/postgres/init/002_schema.sql`
- 新增 `deploy/postgres/init/003_index.sql`
- 落地 `projects`、`documents`、`requirements`、`symbols`、`symbol_edges`、`commits`、`commit_symbols`、`document_links`、`model_profiles`、`query_logs`
- 新增中文数据字典文档 `docs/specs/architecture/05-数据库核心表结构与数据字典.md`
- 新增执行计划文档 `docs/plans/execution/13-DB-01-核心表结构设计与初始化脚本计划.md`

## 验证结果

- 使用一次性 PostgreSQL 16 空库容器执行 `001_bootstrap.sql`、`002_schema.sql`、`003_index.sql` 成功
- 已确认 `app` schema 下 11 张目标表全部存在
- 已确认关键索引存在，包括：
  - `idx_symbols_project_symbol_name`
  - `idx_symbol_edges_project_source`
  - `idx_commits_project_commit_time`
  - `idx_query_logs_project_created_at`
- 已确认 `app.platform_bootstrap_marker` 中平台标记值为 `autocode`

## 过程说明

- 首次验证时，`pg_isready` 过早返回，导致在初始化脚本尚未全部执行完时读取了部分表结构
- 复验时改为按目标表数量轮询等待，确认问题出在验证时机而非 SQL 本身

## 当前结论

- `DB-01` 已完成
- 当前可严格按顺序进入 `DB-02` Redis 键规范与缓存策略
