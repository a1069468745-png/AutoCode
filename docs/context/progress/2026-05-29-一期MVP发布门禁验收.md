# 2026-05-29 一期 MVP 发布门禁验收报告

## 验收日期
2026-05-29

## 验收范围
一期 MVP 数据与服务接入阶段全部必做模块

## 模块验收矩阵

| 模块 | 任务 | 状态 | 验证方式 |
|------|------|------|----------|
| 基础设施 | INF-01~05 | ✅ | 目录结构、构建、部署骨架完整 |
| 数据库基线 | DB-01~02 | ✅ | Schema SQL、Redis 键规范就位 |
| API Gateway | GW-01~04 | ✅ | 6 个安全测试通过 |
| Project Service | PS-01~03 | ✅ | 13 个集成测试通过 |
| CodeGraph Runner | CG-01 | ✅ | 工程骨架 + 测试通过 |
| History Indexer | HI-01 | ✅ | 工程骨架 + 测试通过 |
| Knowledge Indexer | KI-01 | ✅ | 工程骨架 + 测试通过 |
| Context Service | CS-01~07 | ✅ | 33 个测试通过 |
| LLM Gateway | LLM-01~03 | ✅ | 占位工程编译打包通过 |
| Web Console | FE-01~06 | ✅ | 前端构建通过 |
| 运维基线 | OPS-01 | ✅ | 全部服务 health/metrics/logging 就位 |
| 部署基线 | REL-01 | ✅ | Docker Compose config 验证通过 |

## 门禁验证

### 后端测试
- `mvn -pl project-service -am test`: 13 tests ✅
- `mvn -pl api-gateway -am test`: 6 tests ✅
- `mvn -pl context-service -am test`: 33 tests ✅
- `mvn -pl codegraph-runner -am test`: 1 test ✅
- `mvn -pl history-indexer -am test`: 1 test ✅
- `mvn -pl knowledge-indexer -am test`: 1 test ✅

### 前端构建
- `pnpm.cmd --dir frontend/web-console build`: ✅

### 部署验证
- `docker compose -f deploy/docker-compose.yml config`: ✅

### 烟幕测试
- `integration-tests/run-mvp-smoke.ps1`: ✅ MVP smoke suite passed

## 本次新增能力

### LLM-01/02/03 LLM Gateway 占位工程
- 模型配置 CRUD (`POST/PUT/GET/DELETE /api/llm/projects/{id}/model-profile`)
- OpenAI-compatible 占位接口 (`POST /v1/chat/completions`)
- 调用审计日志 (`app.query_logs`)
- 配置管理 (`autocode.llm.*`)

### GW-04 审计与限流
- 令牌桶限流过滤器 (`RateLimitFilter`)
- 可配置限流规则 (`/api/query/` 10rps, `/v1/chat/` 5rps)
- 增强审计日志（已有 `RequestAuditFilter`）

### GW-02 认证增强
- 本地账号登录 (`POST /api/auth/login`)
- HMAC-SHA256 JWT 令牌签发与验证
- 可配置本地用户列表
- 保留 dev-token 开发快捷方式

### OPS-01 可观测性
- 全部 7 个服务接入 Actuator（health/info/metrics）
- 就绪探针与存活探针 (`management.endpoint.health.probes.enabled`)
- 统一结构化日志格式（含 traceId 占位）
- 应用标签指标 (`management.metrics.tags.application`)

## 待完成项（一期剩余）

| 任务 | 说明 |
|------|------|
| CG-02~04 | CodeGraph Runner 完整索引管线（当前 project-service SyncIndexes 替代） |
| HI-02~04 | History Indexer 完整索引管线 |
| KI-02~04 | Knowledge Indexer 完整索引管线 |
| IT-01~04 | 单模块索引链路联调测试 |
| PS-04 | 异步索引触发与项目状态机 |
| REL-02 | 完整 MVP 门禁报告（含索引管线验收） |

## 结论
一期 MVP 核心服务已全部就位，基础设施、数据库、网关、项目服务、上下文服务、前端和 LLM 网关均通过门禁验证。索引服务（CG/HI/KI）当前为工程骨架，实际索引能力通过 project-service 的 SyncIndexes 实现。建议后续优先推进索引服务完整管线。
