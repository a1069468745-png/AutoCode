# 2026-05-29 真实索引数据联调打通进度

## 本轮目标
- 让浏览器联调不再只返回 `EMPTY`
- 把当前工作区的真实代码、Git 历史和 Markdown 文档同步进 PostgreSQL
- 修复前后端联调中“有链路但无真实数据”的最后缺口

## 完成内容
- 为 `project-service` 增加 `POST /api/projects/{id}/sync-indexes`
- 增加本地工作区真实索引同步逻辑：
  - 代码符号扫描并写入 `app.symbols`
  - 基于 import 关系构建基础 `app.symbol_edges`
  - Git 提交与文件变更映射写入 `app.commits` 和 `app.commit_symbols`
  - Markdown 文档、需求码和文档链接写入 `app.documents`、`app.requirements`、`app.document_links`
- 修复 `context-service` 查询适配器与真实表字段不一致的问题
- 将 `DefaultSqlQueryExecutor` 从占位实现切换为真实 JDBC 查询
- 前端项目管理页增加 `Sync indexes` 按钮
- 补充浏览器验证与本地 schema 初始化说明文档

## 真实联调结果
- 当前项目：`Browser Demo Project`
- 同步结果：
  - `symbolCount = 825`
  - `edgeCount = 7547`
  - `commitCount = 16`
  - `documentCount = 8`
  - `requirementCount = 9`
  - `linkCount = 110`
- 查询验证：
  - `POST /api/query/code` with `ContextQueryController` 返回 `SUCCESS`
  - `POST /api/query/history` with `feat` 返回 `SUCCESS`
  - `POST /api/query/knowledge` 已返回真实文档链接结果

## 本轮补充的环境动作
- 对本地 PostgreSQL 执行：
  - `deploy/postgres/init/002_schema.sql`
  - `deploy/postgres/init/003_index.sql`
- 清理 Redis 中旧的 `context:query:cache:*` 查询缓存，避免索引前空结果污染浏览器验收

## 验证
- `mvn -pl project-service -am test -f backend/pom.xml`
- `mvn -pl context-service -am test -f backend/pom.xml`
- `pnpm.cmd --dir frontend/web-console build`
- 浏览器页确认：
  - `Project Management` 中项目状态为 `READY`
  - `Query Playground` 已绑定 `Browser Demo Project`

## 结论
真实索引数据链路已经打通。当前浏览器联调从“接口可通但结果为空”推进到了“本地真实工作区可同步、后端真实命中可返回、浏览器页面可直接触发同步”。
