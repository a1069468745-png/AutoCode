# 当前任务状态
## 当前主任务
`索引类服务初始化任务推进`

## 当前状态
- 状态：`进行中`
- 风险等级：`中低`
- 是否阻塞：`否`

## 当前已完成
- 已完成治理基线阶段验收并形成执行闭环
- 已完成 Docker 基础组件初始化验证
- 已完成 `INF-01` 仓库结构初始化
- 已完成 `INF-02` Maven 多模块工程骨架
- 已完成 `INF-03` 前端工程骨架
- 已完成 `INF-04` 统一配置与环境变量规范
- 已完成 `INF-05` Docker Compose 部署骨架扩展
- `deploy/` 已纳入 `web-console`、`api-gateway` 与 7 个后端占位服务
- 当前部署骨架已具备正式进入 `DB-*` 与服务初始化任务的承载能力
- 已完成 `DB-01` 核心表结构设计与初始化脚本
- 已落地 `deploy/postgres/init/002_schema.sql` 与 `003_index.sql`
- 已完成空库初始化验证，确认核心表与关键索引可顺序执行成功
- 已完成 `DB-02` Redis 键规范与缓存策略
- 已落地 `docs/specs/architecture/06-Redis键规范与缓存策略.md`
- 已固化项目元数据、热点查询、任务状态与会话上下文四类缓存规则
- 已完成 `GW-01` API Gateway 工程初始化
- 已落地可运行 Spring Boot 网关骨架、`Bearer dev-token` 鉴权占位与统一 `401` JSON 响应
- 已完成 `mvn -q -pl api-gateway -am test` 与 `mvn -q -DskipTests package` 验证
- 已完成 `PS-01` Project Service 工程初始化
- 已落地 `POST /api/projects`、`GET /api/projects`、`GET /api/projects/{id}` 三个最小接口
- 已接入 `app.projects` 持久化与 Redis 项目元数据缓存
- 已完成 `mvn -q -pl project-service -am test` 与 `mvn -q -DskipTests package` 验证
- 已完成 `CG-01` CodeGraph Runner 工程初始化
- 已落地 `codegraph-runner` Spring Boot 启动入口、`autocode.codegraph` 配置属性和受控工作目录准备服务
- 已补齐工作目录越界防护与上下文加载测试
- 已完成 `mvn -pl codegraph-runner -am test` 验证
- 已完成 `HI-01` History Indexer 工程初始化
- 已落地 `history-indexer` Spring Boot 启动入口、`autocode.history` 配置属性、最小扫描范围约束和受控 Git 工作区准备服务
- 已补齐扫描范围校验测试、工作区越界防护测试与上下文加载测试
- 已完成 `mvn -pl history-indexer -am test` 验证

## 当前待完成
- 启动 `KI-01` Knowledge Indexer 工程初始化
- 继续推进 `CS-*`、`LLM-*` 等后续服务初始化

## 下一步建议
- 严格按顺序进入 `KI-01`，启动 Knowledge Indexer 工程初始化
- 在 `KI-01` 后继续推进 `CS-*`、`LLM-*`，形成首批索引与上下文联动服务骨架
- 保持 `deploy/`、后端骨架和前端骨架的回归验证能力
