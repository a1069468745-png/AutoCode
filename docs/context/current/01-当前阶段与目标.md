# 当前阶段与目标
## 当前阶段

`一期 MVP 数据与服务接入阶段`

## 当前目标

- 固化数据库、缓存、网关与项目服务的上游基础
- 完成索引相关基础服务初始化并切换到 `CS-01` 准备阶段
- 保持既有部署骨架、后端骨架与前端骨架稳定可回归验证

## 当前阶段完成标准

- `DB-01` 核心表结构与初始化 SQL 已落地并验证通过
- `DB-02` Redis 键命名、TTL 与失效策略已明确
- `GW-01` 已具备统一入口、鉴权占位与最小测试闭环
- `PS-01` 已具备项目创建、列表、详情和最小缓存闭环
- `CG-01`、`HI-01`、`KI-01` 已完成最小工程初始化
- 数据与缓存基线具备被后续 `CS-*` 与 `LLM-*` 服务直接复用的统一入口

## 阶段退出条件

- `CS-01` 已启动并具备明确计划、边界与研发内自测路径
- 当前阶段核心上游依赖不再需要继续补基础骨架
